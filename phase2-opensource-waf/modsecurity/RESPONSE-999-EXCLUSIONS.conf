# ================================================
# Fichier d'exclusions custom — DVWA
# Auteur : Abdennour
# Date   : Mai 2026
# ================================================
# Ce fichier est chargé APRÈS les règles CRS (RESPONSE-999)
# pour que SecRuleUpdateTargetById fonctionne correctement.

# --- Exclure le champ password du formulaire de login ---
# Le mot de passe contient des caractères spéciaux détectés
# comme SQLi par libinjection (faux positif)
SecRuleUpdateTargetById 942100 "!ARGS:password"
SecRuleUpdateTargetById 942200 "!ARGS:password"
SecRuleUpdateTargetById 942260 "!ARGS:password"
SecRuleUpdateTargetById 942370 "!ARGS:password"
SecRuleUpdateTargetById 942440 "!ARGS:password"
SecRuleUpdateTargetById 941100 "!ARGS:password"
SecRuleUpdateTargetById 941160 "!ARGS:password"
SecRuleUpdateTargetById 920273 "!ARGS:password"

# --- Exclure le champ username ---
SecRuleUpdateTargetById 942100 "!ARGS:username"
SecRuleUpdateTargetById 941100 "!ARGS:username"
